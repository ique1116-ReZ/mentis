services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: mentis_rehab
      POSTGRES_USER: mentis
      POSTGRES_PASSWORD: mentis_dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: mentis
      MINIO_ROOT_PASSWORD: mentis_dev_password
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

volumes:
  postgres_data:
  minio_data:

